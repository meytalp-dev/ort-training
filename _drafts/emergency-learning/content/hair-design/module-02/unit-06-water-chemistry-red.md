# יחידה 2.6 — כימיה בסיסית: מים

```yaml
subject: עיצוב שיער סוג 1
module: מ2 — ביולוגיה, טריקולוגיה וכימיה של השיער
unit_type: task
grade: [לאימות — לפי מסלול המגמה]
track_id: 2
content_source: private-docx
duration_mode: estimated
skill: ידע — כימיה בסיסית, מים
pedagogy_verified: [לאימות — מומחה תחום]
source: תוכנית לימודים — עיצוב שיער סוג 1.docx · סעיף 8 · מודולה מ2
palette_primary: "#C65D8E"
palette_soft: "#FCEBF3"
risk_class: RED — חומרים כימיים
status: [לאימות—מומחה]
```

> רעיון מרכזי אחד: [לאימות—מומחה] תפקיד המים בהקשר מקצועי של השיער.

**חסימה:** שלד פדגוגי בלבד. אין למסור לתלמיד לפני חתימת מומחה תחום.

---

## 🟢 לאט ובטוח (`basic`)
**פתיח:** לקוחה שואלת כיצד מים קשורים לשירות.

**הסבר:** [לאימות—מומחה: עובדה אחת ממסמך המקור בלבד]

**דוגמה:** כרטיס שירות מתעד את ההקשר הרלוונטי. [לאימות—מומחה]

**שאלות (2):**
1. שאלה אמריקאית על המושג המאומת.
   - תשובה נכונה: [לאימות—מומחה]
   - מסיח: [לאימות—מומחה] — `error_type: concept`
   - מסיח: [לאימות—מומחה] — `error_type: attention`
2. שאלה אמריקאית על יישום המושג.
   - תשובה נכונה: [לאימות—מומחה]
   - מסיח: [לאימות—מומחה] — `error_type: rule-application`
   - מסיח: [לאימות—מומחה] — `error_type: overgeneralization`

**סיכום:** [לאימות—מומחה]

---

## 🔵 כרגיל (`standard`)
**פתיח:** אותו מושג נבדק בתוך תרחיש סלון.

**הסבר:** [לאימות—מומחה: שכבה שנייה בלבד]

**דוגמה:** [לאימות—מומחה: תרחיש לקוח ללא הוראות חומר]

**שאלות (3):**
1. בחירה מרובה — [לאימות—מומחה + EPA לכל מסיח].
2. בחירה מרובה — [לאימות—מומחה + EPA לכל מסיח].
3. תרחיש קצר — **מחוון:** [לאימות—מומחה].

**סיכום:** [לאימות—מומחה]

---

## 🟣 בא לי אתגר (`advanced`)
**פתיח:** משווים שני מצבים מקצועיים.

**הסבר:** [לאימות—מומחה: ללא יחס, זמן, ריכוז או הוראת שימוש]

**דוגמה:** [לאימות—מומחה]

**שאלות (3):**
1. בחירה מרובה — [לאימות—מומחה + EPA].
2. ניתוח מקרה — **מחוון:** [לאימות—מומחה].
3. ניסוח שורת כרטיס שירות — **מחוון:** [לאימות—מומחה].

**סיכום:** [לאימות—מומחה]

### הערת אימות
[לאימות—מומחה תחום; חסום לתלמיד]