# רֶצֶף — הנחיה לסוכן הבא (17.7.2026)

> הדבק את הקובץ הזה בתחילת שיחה חדשה. ריפו פרטי `meytalp-dev/ort-training`, הכול ב-`_drafts/emergency-learning/`.
> **קרא קודם:** `content/SESSION-KICKOFF.md` (הפייפליין) · `content/unit-dna.md` (ה-DNA) · `content/refutation-log.md` (הבקלוג).

---

## המשימה: להשלים את חילוץ הכשירויות מהמקור

### הרקע — למה זה קריטי
`curriculum-data.js` הוא **מקור-האמת של כל הפייפליין** — ממנו נגזרים תתי-הנושאים, מוני הלוח, והפרומפטים ל-GPT. הוא חולץ ב-16.7 מקבצי DOCX **עם באג פיצול**:

```
במקור:  "עקרונות בסיסיים: נקודה, קו, צורה, קומפוזיציה; ההבחנה בין וקטור לפיקסל; ..."
                          └────── פריט אחד ──────┘     ↑ כאן מפרידים

הבאג:   פיצל על פסיק *וגם* על ';'  →  כשירות אחת רוסקה ל-4 "תתי-נושאים"
        + השמיט פריטים קצרים       →  "קו" נעלם מהנתונים לגמרי
```

**הכלל הנכון: מפרידים על `;` ועל סוף-משפט. לעולם לא על פסיק.**

הנזק: 1,803 תתי-נושאים מנופחים במקום ~1,500. GPT הפיק יחידות שלמות לשברים ("מימון", "צורה", "עדות").

### מה כבר נעשה
1. **`fix-curriculum-split.js`** — תיקון מכני שאיחה 119 מקרים של **סוגריים** לא מאוזנים (`"...(תמחור"` + `"מרוכזות)"`). 279 פרגמנטים הוסרו. **זה כבר ב-`curriculum-data.js` הנוכחי.** תיקון-ביניים, לא הפתרון.
2. **`extract-curriculum.py`** — המחלץ הנכון, ישירות מה-DOCX. **עובד ל-16/22 מקצועות · 813 תתי-נושאים.** אומת: `"עקרונות בסיסיים: נקודה, קו, צורה, קומפוזיציה"` חוזר שלם עם `"קו"`.
3. **`scan-giant-subtopics.js`** — מאתר תת-נושא-ענק (דפוס-על 0) + פיצולי-נקודתיים.

### 🎯 מה נשאר לעשות — לפי סדר

**שלב 1 — להשלים את המחלץ ל-22/22.**
מקור: `C:/Users/meyta/Downloads/תוכניות לימודים-20260715T185021Z-1-001.zip` (57 DOCX).
הרצה: `python extract-curriculum.py` (יבש) · `--write` (כותב).

נכשלים כרגע (**"לא נמצאו מודולות"**): `football` · `ict-systems` · `cooling-hvac` · `english` · `history-arabic` · `history-diploma`
מחזירים מודולות **בלי תוכן**: `driving-instruction` (0) · `hebrew` (1) · `tanach` (0)

**הסיבה תמיד מבנה-דוקס שונה, וההבדלים קטנים.** דוגמה שכבר נפתרה: שיער נכשל כי הוא כותב `מודולה מ1 —` (קידומת עברית) ולא `מודולה 1 —`, והעמודה נקראת `פירוט` ולא `תוכן`. תיקון של תו אחד ברג'קס פתר 3 מקצועות.
**שיטה:** לכל מקצוע שנכשל — לדמפ את שורות ה-DOCX, למצוא איך המודולה והשדות מסומנים, להרחיב את `MOD_RX` / `FIELDS` / `TABLE_HDR`. אל תכתוב פרסר נפרד לכל קובץ; הרחב את הקיים.

**שלב 2 — השוואה לפני החלפה. חובה.**
`curriculum-data.js` הוא מקור-אמת. **אל תחליף אותו בלי דיף מודולה-מודולה שמיטל רואה.**
- להשוות `.curriculum-new.json` מול `curriculum-data.js` הנוכחי.
- להציג: כמה תתי-נושאים לכל מודולה לפני/אחרי · אילו פריטים חדשים (שאבדו וחזרו) · אילו נעלמו.
- גיבוי לפני כתיבה. לאמת אחרי: 22 מקצועות נטענים · `R.greenQueue().length` נשאר 140 · הלוח עולה.

**שלב 3 — לרענן ולהצליב.**
`bash content/refresh.sh` → מסנכרן `produced.js` מהדיסק, סורק ענפים יתומים וקישורים שבורים.
אחר כך: אילו מ-312 היחידות הקיימות נכתבו לשברים שכבר לא קיימים? **לא למחוק — לדווח למיטל.**

---

## ⛔ כללי-ברזל — הכי חשוב בקובץ

- **`_drafts/emergency-learning/` בלבד.** לא לגעת ב-`docs/`.
- **לא למחוק קבצים מהריפו בלי אישור מפורש של מיטל.** קרה: יחידה עם 277 שורות תוכן אמיתי נמחקה כי סוננה **לפי הכותרת** ולא לפי הגוף. **קרא את הגוף לפני שאתה מוחק.**
- **אתה בודק, לא יצרן.** GPT מפיק ודוחף לבד. Claude מפריך (7 צירים) ומתעד ב-`refutation-log.md`. **יצרן ≠ בודק על אותה יחידה.**
- **הכול `[לאימות]` עד חתימה אנושית.** אף מודל לא מסמן `pedagogy_verified`.
- **RED חסום** (בטיחות · כימיה · מיסוי/משפט · רפואה). DNA §6 = תקרת-LLM: מומחה אנושי, לא Perplexity. **לא לעקוף.**
- commit+push אוטומטי, **בעברית**. GPT דוחף במקביל → אם push נדחה: `git pull --no-rebase` ואז push.
- **אל תסכים למיטל אוטומטית.** היא ביקשה שיפוט עצמאי. אמור כשהיא צודקת וכשלא.

## 🪤 המלכודת שחזרה 3 פעמים ביום אחד — אל תיפול בה

**לפני שאתה מאשים את GPT — בדוק מה הפרומפט או ה-DNA אמרו לו לעשות.**
שלושה false positives ביום אחד, בכולם GPT צדק:
1. **"המצאת מקור"** — האשמתי אותו שהמציא `DOCX · סעיף 8`. בפועל `buildPrompt` מוסר לו את שם ה-DOCX (`subj.src`), `taxRef` של כל המגמות הוא `§8`, ו-**DNA §1a דורש במפורש** `source` = מסמך + סעיף. הוא העתיק לפי התקן.
2. **"חסר error_type"** — כמעט סימנתי כבאג. ביחידות **ביצוע** אין מסיחים לתייג. ההיעדר תקין.
3. **"קישורים שבורים במניפסט"** — 7 "שבורים" בתקשוב מ7. המניפסט כותב `— מתוכנן —` ליחידות עתידיות. תוכן-עניינים, לא שקר.

**כל הבאגים האמיתיים של אותו יום היו בתשתית שלנו, לא ב-GPT:** ספירת הלוח, חסם "כתבי המשך", `produced.js` מיושן, כפתור העתקה חסר, וחילוץ הכשירויות.

## ✅ מה שכן נמצא נגד התוכן — אמיתי ומאושש
**דפוס-על 0 — תת-נושא-ענק.** כדורגל מ4: `[ידע] חוקת המשחק לעומקה` הוא תת-נושא **אחד** → GPT הפיק יחידה **אחת** → **חוק אחד מתוך 17** (אומת מול IFAB: *"לא נאות כלל ככיסוי להכשרת שופטים"*). GPT ציית לכלל בדיוק; **הכלל "תת-נושא = יחידה" נשבר כשתת-הנושא הוא תחום שלם.**
ראה `football/module-04/_verification.md` · `media-design/module-06/_verification.md`.

---

## מצב (17.7)
- **312 יחידות** ב-main. `refresh.sh` נותן את המספר המדויק תמיד — **אל תסמוך על מספר בקובץ.**
- **מלאות וממתינות:** מדיה מ1/מ2/מ3/מ6/מ8/מ9/מ10 · תקשוב מ3 · שיער מ1/מ2/מ4/מ5/מ7/מ8/מ9 · לוג_יטק מ1/מ3/מ5.
- **בטוח להריץ** (הרשימות לא נגועות): מדיה מ5 · תקשוב מ7 · תקשוב מ9 · טקסטיל מ5.
- **⛔ אסור להריץ עד סוף שלב 2:** **כל הליבה** — היסטוריה · ספרות · אזרחות · עברית · מתמטיקה. הפיצול על נקודתיים פגע בהן הכי חזק.
- **ענף יתום:** `content/confectionery-m3-gpt` — 13 יחידות. **החלטת מיטל: לא למזג.** להשאיר.
- **חוב ידוע:** 29 יחידות עם `רמה: מתקשה` גלוי (מפר DNA §0 — התלמיד לא רואה תווית רמה). תיקון מכני, ממתין לאישור.

## מה חוסם ואיננו טכני
- **רכז מגמת עיצוב-מדיה** — 8 מודולות מלאות ברף גבוה ממתינות. **צוואר הבקבוק כבר לא ייצור.**
- **ממונה בטיחות** (ריתוך מ1 · שיער מ1/מ2/מ5) · **מומחה בטיחות מזון** (קונדיטאות מ2).
- **החלטת מיטל:** אימות פדגוגי מחליף חתימה בשלב זה; חתימה תבוא מאוחר יותר.
