# Edura · סוכן איסוף משרות — מדריך הקמה

**מטרה:** לאסוף משרות הוראה מ-14 מקורות, לראות מה יש בשטח, לעצב את האתר על בסיס הנתונים האמיתיים.

---

## שלב 1 — יצירת ה-Sheet (5 דקות)

1. כנסי ל-[Google Sheets](https://sheets.google.com) ולחצי **"+"** ליצירת גיליון חדש
2. שם הגיליון: **`Edura — מאגר משרות`**
3. שמרי את הקישור — נצטרך אותו לאחר מכן

---

## שלב 2 — העתקת הסקריפט (5 דקות)

1. בתוך ה-Sheet החדש: **Extensions → Apps Script**
2. תיפתח לשונית חדשה עם עורך קוד
3. **מחקי הכל** (כל הקוד הקיים `function myFunction() {}`)
4. הדביקי את כל התוכן של [jobs-scanner-apps-script.js](jobs-scanner-apps-script.js)
5. **שמרי:** Ctrl+S (או Cmd+S במאק)
6. שם הפרויקט: שני ל-**`Edura · Jobs Scanner`**

---

## שלב 3 — הרצה ראשונה (10 דקות)

### א. setup — יצירת הטאבים

1. בתפריט העליון, ליד הכפתור "Run" — בחרי בפונקציה **`setup`**
2. לחצי **Run** (▶)
3. בפעם הראשונה Google תבקש הרשאות:
   - **Authorize access**
   - בחרי בחשבון Google שלך
   - "Advanced" → "Go to Edura · Jobs Scanner (unsafe)" → "Allow"
4. אישור: ייווצרו 4 טאבים בגיליון: `jobs`, `seen`, `log`, `stats`

### ב. runScan — איסוף ראשון

1. בחרי בפונקציה **`runScan`**
2. לחצי **Run** (▶)
3. ייקח 30-90 שניות (סורק 14 מקורות)
4. בטאב `log` תראי שורות של מה נסרק והאם הצליח

### ג. analyzeData — סיכום

1. בחרי בפונקציה **`analyzeData`**
2. לחצי **Run** (▶)
3. תקפוץ הודעה עם הסיכום:
   - סה"כ משרות נאספו
   - פילוח לפי מקור
   - פילוח לפי אזור
   - פילוח לפי תפקיד

---

## שלב 4 — הפעלה אוטומטית (2 דקות)

### א. installDailyTrigger — הרצה אוטומטית פעמיים ביום

1. בחרי בפונקציה **`installDailyTrigger`**
2. לחצי **Run** (▶)
3. הסוכן ירוץ אוטומטית:
   - **06:00 בבוקר** (שעון ישראל)
   - **18:00 בערב** (שעון ישראל)

### ב. אימות שהטריגר הותקן

1. בעורך Apps Script — בצד שמאל, לחצי על אייקון השעון ⏰ (Triggers)
2. תראי 2 טריגרים מותקנים על `runScan`

---

## שלב 5 — Web App (לאתר Edura — שלב מאוחר יותר)

⚠️ **לא חובה עכשיו.** רלוונטי רק כשנבנה את האתר.

1. **Deploy → New deployment**
2. סוג: **Web app**
3. הגדרות:
   - **Description:** Edura Jobs API v1
   - **Execute as:** Me
   - **Who has access:** Anyone
4. **Deploy**
5. העתיקי את ה-Web app URL — זה ה-API שיציג משרות באתר Edura

---

## פונקציות עזר

| פונקציה | מה היא עושה | מתי להשתמש |
|---|---|---|
| `runScan` | סריקה מלאה של 14 מקורות | אוטומטי 06:00 + 18:00, או ידני בכל זמן |
| `analyzeData` | סיכום של מה שנאסף | בכל פעם שאת רוצה לראות סטטיסטיקות |
| `setup` | יצירה ראשונית של הטאבים | פעם אחת בלבד |
| `installDailyTrigger` | התקנת הרצה אוטומטית | פעם אחת בלבד |
| `resetSeen` | מחיקת רשימת URLs שכבר נראו | אם רוצה לסרוק הכל מחדש (מבלי למחוק את ה-jobs) |
| `resetAll` | מחיקת כל המשרות שנאספו | התחלה נקייה לחלוטין |

---

## מבנה הטאבים

### jobs
כל משרה שנמצאה — שורה אחת. עמודות:
- `first_seen` · מתי נראתה לראשונה
- `source_id` · מזהה מקור (jlm-menhai, ort, ...)
- `source_name` · שם מקור בעברית
- `title` · כותרת המשרה
- `url` · קישור למודעה
- `snippet` · קטע טקסט סביב המודעה
- `detected_region` · אזור שזוהה (ירושלים, מרכז, צפון...)
- `detected_subjects` · מקצועות שזוהו (מתמטיקה, אנגלית...)
- `detected_levels` · שכבות שזוהו (יסודי, חט"ב, תיכון)
- `detected_role` · תפקיד שזוהה (מורה, רכז, מחנך...)
- `matched_keywords` · כל מילות המפתח שהיו במודעה
- `is_negative` · האם סומנה כשלילית (התנדבות, מחפש עבודה וכו')

### seen
URLs שכבר נראו — למניעת כפילויות

### log
תיעוד של כל סריקה — מה הצליח, מה נכשל, כמה זמן

### stats
היסטוריה של `analyzeData` — מאפשר לעקוב אחרי הצמיחה לאורך זמן

---

## פתרון תקלות

### "הסקירה לא רצה" או "אין נתונים חדשים"
- כנסי לטאב `log`
- חפשי שורות `scan-error` — שם תראי איזה מקור נכשל
- ייתכן שאתר שינה את המבנה שלו, או חוסם ביוטים

### "Google שלח לי מייל על שגיאה"
- מקובל. Apps Script שולח התראה כשטריגר נכשל
- תיקון: ראי בטאב `log` מה השגיאה
- אם זה מקור ספציפי — אפשר להעיף אותו מהרשימה ב-`SOURCES`

### "המשרות שאני רואה לא רלוונטיות"
- זה נורמלי בשלב הזה — אנחנו אוספים רחב ולא מסננים בחוזקה
- אחרי 24-48 שעות נריץ `analyzeData` ונשפר את הסינון

---

## מה הלאה

1. ✅ הקמת הסוכן (אנחנו כאן)
2. ⏳ איסוף 24-48 שעות
3. ⏳ הרצת `analyzeData` לסיכום
4. ⏳ עיצוב האתר לפי מה שיש בנתונים האמיתיים
5. ⏳ חיבור האתר ל-API של הסוכן
